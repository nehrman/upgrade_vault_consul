---

  - name: "Upgrade consul backend"
    hosts: consul_instances
    become: true
    serial: 1
    roles:
      - role_consul_upgrade

  # - name: "Upgrade vault service THA"
  #   hosts: tha_vault_servers
  #   become: true
  #   serial: 1
  #   roles:
  #     - role_vault_upgrade

